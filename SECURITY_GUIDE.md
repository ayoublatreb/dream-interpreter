# 🔒 دليل الأمان - Security Guide

## الحماية المضافة

### 1. **Rate Limiting (تحديد معدل الطلبات)**
- ✅ **General Limiter**: 100 طلب كل 15 دقيقة لكل IP
- ✅ **API Limiter**: 20 طلب API كل 15 دقيقة لكل IP
- ✅ **Audio Upload Limiter**: 10 رفع ملفات صوتية كل ساعة لكل IP

### 2. **Input Validation (التحقق من المدخلات)**
- ✅ التحقق من طول النص (2-5000 حرف)
- ✅ حماية من XSS (Cross-Site Scripting)
- ✅ تنظيف المدخلات من المحتوى الخطير
- ✅ التحقق من نوع وحجم الملفات

### 3. **File Upload Security (أمان رفع الملفات)**
- ✅ تحديد حجم الملف (10MB كحد أقصى)
- ✅ التحقق من نوع الملف (audio فقط)
- ✅ تنظيف أسماء الملفات من الأحرف الخطيرة
- ✅ حذف الملفات المؤقتة بعد المعالجة

### 4. **CORS Protection (حماية CORS)**
- ✅ تحديد Origins المسموحة فقط
- ✅ رفض الطلبات من مصادر غير معروفة في الإنتاج

### 5. **Security Headers (رؤوس الأمان)**
- ✅ Helmet.js لإضافة رؤوس أمان HTTP
- ✅ Content Security Policy
- ✅ XSS Protection
- ✅ Frame Options

### 6. **Error Handling (معالجة الأخطاء)**
- ✅ عدم كشف تفاصيل الأخطاء الداخلية في الإنتاج
- ✅ رسائل خطأ آمنة للمستخدمين
- ✅ تسجيل الأخطاء للتحليل

### 7. **API Key Protection (حماية مفتاح API)**
- ✅ تخزين API keys في متغيرات البيئة
- ✅ التحقق من وجود API key قبل التشغيل
- ✅ عدم كشف API keys في الأخطاء

## الإعدادات المطلوبة

### ملف `.env`

أنشئ ملف `.env` في مجلد `backend/`:

```env
# OpenAI API Key (مطلوب)
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=production

# Security Configuration
# قائمة Origins المسموحة (مفصولة بفواصل)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: API Key إضافي للحماية
# API_KEY=your_optional_api_key_here
```

## التثبيت

```bash
cd backend
npm install
```

## الحماية من سرقة API

### 1. **استخدام Environment Variables**
- ✅ لا تضع API keys في الكود
- ✅ استخدم `.env` للمفاتيح الحساسة
- ✅ أضف `.env` إلى `.gitignore`

### 2. **Rate Limiting**
- ✅ يمنع الاستخدام المفرط للـ API
- ✅ يحمي من هجمات DDoS
- ✅ يقلل التكاليف

### 3. **Input Validation**
- ✅ يمنع حقن الكود الخبيث
- ✅ يحمي من XSS attacks
- ✅ يحد من حجم البيانات المعالجة

### 4. **CORS Protection**
- ✅ يسمح فقط للمواقع المعتمدة
- ✅ يمنع الوصول غير المصرح به

### 5. **Request Size Limits**
- ✅ يمنع رفع ملفات كبيرة
- ✅ يحمي من هجمات Buffer Overflow

## أفضل الممارسات

### 1. **في الإنتاج:**
- ✅ استخدم `NODE_ENV=production`
- ✅ حدد `ALLOWED_ORIGINS` بدقة
- ✅ راجع الـ logs بانتظام
- ✅ استخدم HTTPS فقط

### 2. **مراقبة الأمان:**
- ✅ راقب Rate Limit violations
- ✅ راقب الأخطاء غير العادية
- ✅ راقب استخدام API

### 3. **التحديثات:**
- ✅ حدث المكتبات بانتظام
- ✅ راقب الثغرات الأمنية
- ✅ استخدم `npm audit` بانتظام

## فحص الأمان

```bash
# فحص الثغرات في المكتبات
npm audit

# إصلاح الثغرات تلقائياً
npm audit fix
```

## ملاحظات مهمة

1. **لا تشارك ملف `.env`** أبداً
2. **استخدم HTTPS** في الإنتاج
3. **راجع الـ logs** بانتظام
4. **حدث المكتبات** بانتظام
5. **راقب استخدام API** لتجنب التكاليف الزائدة

---

**تاريخ التحديث**: $(date)
**الإصدار**: 1.0.0

