# 🚀 دليل التطوير والنشر - تطبيق تفسير الأحلام

دليل شامل لتطوير ونشر وصيانة تطبيق تفسير الأحلام.

---

## 📋 المتطلبات

### متطلبات التطوير
```
Node.js: 16.x أو أحدث
npm: 8.x أو أحدث
yarn: اختياري
Git: للتحكم في الإصدارات
```

### متطلبات الوقت
- 2-3 ساعات للإعداد الأولي
- 30-45 دقيقة للتطوير اليومي
- 1 ساعة للنشر

---

## 🛠️ الإعداد الأولي

### الخطوة 1: استنساخ المشروع
```bash
# استنساخ من Git (أو فك الضغط المحلي)
git clone <repository-url>
cd hamid\ dream
```

### الخطوة 2: تثبيت المكتبات - Frontend
```bash
cd frontend
npm install
# أو
yarn install
```

### الخطوة 3: تثبيت المكتبات - Backend
```bash
cd ../backend
npm install
# أو
yarn install
```

### الخطوة 4: إنشاء ملفات البيئة
```bash
# في المجلد الجذر أو في كل مشروع
cp .env.example .env.local
```

---

## ▶️ تشغيل التطبيق

### تشغيل الخادم الخلفي

```bash
# من مجلد backend
npm start

# أو للتطوير مع auto-reload
npm run dev
```

**الناتج:**
```
Server is running on http://localhost:3001
```

### تشغيل الواجهة الأمامية (في terminal جديد)

```bash
# من مجلد frontend
npm run dev

# أو
yarn dev
```

**الناتج:**
```
  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

### الوصول للتطبيق
افتح المتصفح وذهب إلى:
```
http://localhost:5173
```

---

## 📝 بنية المشروع التطويرية

```
hamid dream/
├── frontend/
│   ├── src/
│   │   ├── App.jsx           # المكون الرئيسي
│   │   ├── App.css           # أنماط App
│   │   ├── index.css         # أنماط عامة
│   │   ├── main.jsx          # نقطة الدخول
│   │   ├── DreamInterpreter.css  # أنماط إضافية
│   │   └── assets/           # الأصول
│   ├── public/
│   │   ├── manifest.webmanifest
│   │   ├── dream-icon.svg
│   │   ├── sw.js            # Service Worker
│   │   └── ...
│   ├── index.html
│   ├── vite.config.js
│   ├── package.json
│   └── ...
│
├── backend/
│   ├── server.js            # الخادم الرئيسي
│   ├── package.json
│   └── ...
│
└── Documentation/
    ├── README.md
    ├── DESIGN_GUIDE.md
    ├── COLOR_SYSTEM.md
    ├── USER_GUIDE.md
    └── ...
```

---

## 🔧 الأوامر الأساسية

### Frontend

```bash
# التطوير
npm run dev

# البناء للإنتاج
npm run build

# معاينة الإنتاج
npm run preview

# فحص الأخطاء (إن وجدت)
npm run lint

# تنسيق الكود
npm run format
```

### Backend

```bash
# التطوير
npm run dev

# الإنتاج
npm start

# اختبار
npm test

# فحص الأخطاء
npm run lint
```

---

## 🎨 التعديلات التصميمية

### إضافة لون جديد

في `App.jsx` أضف في `:root`:
```css
--new-color: #yourcolor;
```

ثم استخدمه:
```css
color: var(--new-color);
```

### تعديل Animation

في `App.jsx` عدّل keyframes:
```css
@keyframes my-animation {
  0% { /* start */ }
  100% { /* end */ }
}
```

### إضافة مكون جديد

1. أنشئ مكون جديد في `src/components/`
2. أضف أنماطه في `DreamInterpreter.css`
3. استوردها في `App.jsx`

---

## 🧪 الاختبار

### اختبار يدوي

```bash
# اختبر جميع الأوضاع
1. وضع التسجيل الصوتي
2. وضع الكتابة
3. وضع العرض
```

### اختبار الاستجابة

```bash
# في Chrome DevTools
1. F12 → Device Toolbar
2. اختبر: Mobile, Tablet, Desktop
3. تحقق من التخطيط
```

### اختبار الأداء

```bash
# Lighthouse في Chrome
1. F12 → Lighthouse
2. اختبر Performance
3. اختبر Accessibility
```

---

## 📱 PWA والتثبيت

### اختبار PWA محلياً

```bash
# بناء الإنتاج
npm run build

# تشغيل خادم محلي للإنتاج
npm run preview
```

### التحقق من Service Worker

```javascript
// في Console
navigator.serviceWorker.getRegistrations()
  .then(registrations => console.log(registrations))
```

### التثبيت للاختبار

1. افتح `localhost:4173` (preview)
2. اضغط على الأيقونة في شريط العنوان
3. اختر "تثبيت"
4. التطبيق سيكون متاحاً

---

## 🐛 استكشاف الأخطاء

### الخطأ: الميكروفون لا يعمل

```javascript
// في Console، تحقق:
navigator.permissions.query({ name: 'microphone' })
  .then(result => console.log(result.state))
```

**الحل:**
- تحقق من إذن الميكروفون في المتصفح
- أعد تحميل الصفحة
- جرب متصفح آخر

### الخطأ: الخادم لا يستجيب

```javascript
// في Console، جرب:
fetch('http://localhost:3001/api/health')
  .then(r => r.json())
```

**الحل:**
- تحقق من أن الخادم يعمل
- تحقق من المنفذ 3001
- تحقق من CORS إذا لزم

### الخطأ: CSS لا يطبق

```javascript
// تحقق من:
document.getElementById('styles') // يجب أن يكون موجود
```

**الحل:**
- تحقق من تطبيق style tag
- امسح الـ cache
- أعد تحميل الصفحة (Ctrl+Shift+R)

---

## 🚀 النشر

### نشر على Vercel (الموصى به)

```bash
# تثبيت Vercel CLI
npm i -g vercel

# النشر
vercel

# الخطوات:
1. اختر المشروع
2. عيّن جذر frontend
3. سيتم النشر تلقائياً
```

### نشر على Netlify

```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# النشر
netlify deploy --prod

# اختر مجلد البناء: dist/
```

### نشر على خادم VPS

```bash
# بناء الإنتاج
cd frontend
npm run build

# نسخ الملفات
scp -r dist/ user@server:/path/to/public/

# على الخادم
npm start  # لتشغيل backend
```

---

## 📚 ملفات التكوين الهامة

### `vite.config.js` (Frontend)

```javascript
// معلومات الإعدادات الأساسية
// - المنفذ (5173)
// - الـ Base URL
// - Plugins
```

### `package.json` (كلا الطرفين)

```json
{
  "name": "dream-interpreter",
  "version": "1.0.0",
  "dependencies": { /* ... */ },
  "devDependencies": { /* ... */ },
  "scripts": { /* الأوامر */ }
}
```

---

## 🔐 الأمان في التطوير

### متغيرات البيئة

```bash
# في .env.local
VITE_API_URL=http://localhost:3001
VITE_APP_ENV=development
```

### CORS في التطوير

```javascript
// في backend/server.js
app.use(cors({
  origin: 'http://localhost:5173',
  credentials: true
}))
```

---

## 📊 مراقبة الأداء

### في Development Mode

```bash
# استخدم React DevTools
# Chrome DevTools
# Network tab للتحقق من الطلبات
```

### في Production Mode

```bash
# استخدم Lighthouse
# WebPageTest
# Google Analytics
```

---

## 🔄 عملية التطوير الموصى بها

### 1. إنشاء فرع جديد
```bash
git checkout -b feature/my-feature
```

### 2. التطوير والاختبار
```bash
npm run dev
# عدّل الكود واختبره
```

### 3. الالتزام بالتغييرات
```bash
git add .
git commit -m "Add: description of changes"
```

### 4. دفع التغييرات
```bash
git push origin feature/my-feature
```

### 5. فتح Pull Request
```
من GitHub اختر: Open Pull Request
```

---

## 📋 قائمة التحقق قبل النشر

- [ ] جميع الأخطاء معالجة
- [ ] الاختبار على Responsive
- [ ] اختبار الأداء (Lighthouse 90+)
- [ ] اختبار على متصفحات متعددة
- [ ] تحديث البناء النهائي
- [ ] اختبار PWA
- [ ] التحقق من متغيرات البيئة
- [ ] توثيق التغييرات

---

## 📞 الدعم التقني

### الموارد المفيدة
- [Vue Docs](https://vuejs.org)
- [Vite Docs](https://vitejs.dev)
- [MDN Web Docs](https://developer.mozilla.org)

### المشاكل الشائعة

**مشكلة**: المشروع بطيء جداً
**الحل**: امسح node_modules وعيد التثبيت

**مشكلة**: الصور لا تحمّل
**الحل**: تحقق من المسار النسبي

**مشكلة**: Hot Reload لا يعمل
**الحل**: أعد تشغيل الخادم

---

## 🎊 الخلاصة

دليل تطوير شامل يغطي:
- ✅ الإعداد الأولي
- ✅ تشغيل التطبيق
- ✅ التعديلات التصميمية
- ✅ الاختبار والتصحيح
- ✅ النشر والنشر
- ✅ الصيانة والدعم

---

**آخر تحديث: 23 ديسمبر 2025**

Happy Coding! 🚀✨
